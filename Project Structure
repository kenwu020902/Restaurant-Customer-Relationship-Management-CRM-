# Create the basic folders (for our restaurant app)
mkdir backend frontend docs
touch README.md  # Already exists if you checked the box

# Initialize npm for backend
cd backend
npm init -y

# Create basic files
touch server.js
touch .gitignore
